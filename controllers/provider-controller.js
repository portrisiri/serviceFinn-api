const providerController = {};

providerController.getProviders = async (req, res, next) => {
  try {
    // code here
  } catch (error) {
    next(error);
  }
};

module.exports = providerController;
